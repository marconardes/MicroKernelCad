package com.cad.modules.geometry.entities;

/**
 * Common interface for all 2D geometric entities in the geometry module.
 * This can be used for polymorphism and to define common contracts later.
 */
public interface GeometricEntity2D {
    // Initially empty, can be populated with common methods like getType(), getBounds(), etc.
}
